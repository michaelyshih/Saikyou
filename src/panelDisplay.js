import THREE from "three";

export default class panelDisplay{
    constructor(panel_id) {
        this.panel_id = panel_id;
        this.video = document.getElementById(panel_id);

    }


};
